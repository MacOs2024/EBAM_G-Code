# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Результаты прогонов регрессии: пересоздаются каждым запуском тестов,
# фиксируются не в репозитории, а в заметках к версии и SHA-манифестах.
*_results.json

# Сборка Windows-дистрибутива
build/
dist/
wheelhouse/

# Локальные настройки и рабочие файлы оператора
*.log
.DS_Store
