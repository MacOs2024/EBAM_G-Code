# v4.2.9.33 PATH ESTIMATE AND SETPOINT DENSITY

- Оценка времени XY-колец/спирали считается по фактической длине траектории, а не по объёму детали.
- Добавлены rotational_layer_path_length_mm() и rotational_total_path_length_mm() в core.py — та же геометрия, что у генератора.
- Спираль считается аналитически (витки x средняя окружность): совпадение 1.000 при скорости в ~100 раз выше выборки точек.
- На тонкой стенке 2 мм занижение времени было в 2.16 раза, на 3 мм — в 1.44; после правки расхождение 0.3%.
- Геометрия протянута через build_settings и _fit_settings_to_target_time: подгонка под целевое время считает той же моделью.
- Без геометрии (STL, обратный анализ) сохраняется прежняя объёмная оценка.
- Добавлена drop_redundant_analog_setpoints(): убирает M67/M68, задающие каналу уже установленное значение.
- Настройка dedupe_analog_setpoints (по умолчанию включена), применяется во всех шести генераторах.
- При выключенном мягком старте убирается 49% команд M68; последовательность значений E0/E2 и число движений не меняются.
- Уставки на перемычках не убираются — там подача выключается физически; подсказка светофора это поясняет.
- Исправлено: блок настроек рампы применялся только при включённой галочке упрощения рампы.
- Добавлен набор path_estimate_and_setpoint_density_test (40 проверок). Регрессия 20/20, failures=0.

# v4.2.9.32 MACHINE CONFIG ALIGNMENT

- Диаметры проволоки приведены к роликам станка ([ROLLER_SCALE]): 0.8/1.2/1.6/2.0/2.4/3.2 мм.
- Профиль Ø2.5 мм удалён — такого ролика на «Бормаше» нет; ключ переносится на Ø2.4 с сообщением оператору.
- Добавлены профили Ø0.8 и Ø3.2; потолок подачи и границы слоя пересчитываются от диаметра автоматически.
- Добавлено предупреждение, если диаметр введён руками и не совпадает ни с одним роликом (не блокировка).
- Добавлен вывод номера слоя `M68 E4` во всех шести генераторах; по умолчанию выключен, т.к. motion.analog-out-04 не подключён в HAL.
- В непрерывном C-режиме номер слоя выдаётся один раз на слой, а не на каждое кольцо.
- Номер слоя всегда идёт как M68, даже при выбранном M67: синхронизация с движением не нужна.
- Оценка времени учитывает разгон/торможение осей: трапецеидальный профиль, переходящий в треугольный на коротком пути.
- Линейное ускорение на поворотном столе считается как a_C·π·R/180 и падает с радиусом; на R=5 мм время было занижено в 3.3 раза.
- Все 14 мест расчёта «путь/скорость» переведены на общую функцию; выключатель возвращает прежнее поведение.
- В генераторе фланцев расчёт задания не изменён (он задаёт ток и подачу), добавлена отдельная честная оценка с разгоном.
- Починены три регрессионных набора, сломанные переупаковкой: пути к эталонной STL и к app.py, устаревшая привязка к версии v4.2.9.19.
- Добавлен набор layer_number_and_accel_regression_test (60 проверок).
- Регрессия 19/19, failures=0; впервые прогнана полностью в контейнере с реальными зависимостями.

# v4.2.9.19 PARAMETRIC FLANGE-FAMILY

- Добавлен отдельный режим `Flange-family R6 (STL → C-кольца)`.
- OD, ID, высота, ось и радиальные уступы извлекаются из STL; размеры Flange1 не зашиты.
- Каждый слой строится от внутреннего радиуса к наружному, C только положительная.
- Расчёт согласует объём, кольца, E0/E2/C и время 4–6 ч с развёрткой 300 Гц X10/Y10.
- Горячий комплект блокируется при нарушении лимитов или геометрических проверок.
- Добавлены dry-run, два коротких теста, CSV/JSON, SHA-256 и CLI-генератор.
- Имена STL очищаются перед записью комментариев, поэтому `Flange1(2).STL` не создаёт вложенный комментарий LinuxCNC.
- Регрессия пройдена на исходной и автоматически изменённой геометрии.

# v4.2.9.18 CYLINDR V5 3-MM QUALIFICATION PACK

- Добавлен воспроизводимый генератор короткого пакета квалификации по исходному JSON Cylindr_V5.
- Пакет содержит сухой motion-файл E0=E2=0 и три 10-мм TEST-варианта E2: −10%, расчёт 3.0 мм, +10%.
- Каждый вариант включает G-code, audit, расширенный layers.csv и settings.json.
- Добавлены матрица, журнал измерений, метаданные с неподтверждёнными machine-checks и SHA-256 manifest.
- Горячий запуск не авторизуется: сначала обязательны dry-run Z/C/HAL и выбор одного TEST оператором.

# v4.2.9.17 SAFE INITIAL Z APPROACH

- Добавлен независимый абсолютный Z для начального позиционирования B/C и XY.
- Опция по умолчанию выключена: старое поведение и импорт прежних JSON сохранены.
- При включении последовательность становится: безопасное состояние E0/E2 → G0 Zsafe → B/C → XY → контролируемый G1 к рабочему Z.
- Low-Z audit учитывает новый подвод и вместо ложной тревоги требует dry-run подтверждение зазора и системы координат.

# v4.2.9.16 ACTUAL E0/E2 COMMAND TRACE

- `layers.csv` сохраняет прежние расчётные `current_ma`/`wire_mm_s` и добавляет фактические команды `actual_e0_command_ma`/`actual_e2_command_mm_s`.
- Для no-pause режима записывается `m68_once_average`, `m67_once_average`, `m68_zone` или `m67_zone`.
- Поле `analog_command_update` отмечает слой, перед которым команда реально отправляется.
- Для генераторов, где команда меняется внутри прохода, новые поля намеренно остаются пустыми с режимом `not_recorded`, чтобы не выдавать расчёт за факт.

# v4.2.9.15 REQUESTED → EFFECTIVE TRACE

- Для no-pause C-генератора добавлен структурированный список неявно изменённых настроек.
- В `stats` и audit видны исходное значение, применённое значение и правило изменения.
- G-code содержит краткий маркер количества изменённых полей без раздувания программы.
- След охватывает радиальный шаг, continuous-стратегию, Z-hop, W-ретракт и все отключаемые паузы.

# v4.2.9.14 VERSION & WIRE-SANITY REGRESSION

- Устранена жёсткая привязка Streamlit AppTest к устаревшей версии v4.2.9.10.
- Версия интерфейса, ядра, JSON-результата UI-теста и Windows-launcher теперь согласована.
- Добавлен отдельный регрессионный набор `wire_sanity_regression_test.py` для ручной E2, сухого прогона, low-Z и расчёта 3-мм стенки.
- Поведение генерации v4.2.9.13 сохранено: новая версия усиливает проверяемость и не меняет G-code автоматически.

# v4.2.9.10 INDUSTRIAL CONTROL QA

- Сопоставлена логика приложения с NASA EBF³, Sciaky EBAM/IRISS и LinuxCNC.
- Внешние overrides оставлены полностью операторскими; M49/M50 не генерируются.
- WIRE override 100% показывается только как рекомендация для сравнимой калибровки.
- Добавлен выбор планировщика: G64 P/Q, machine default, G61, G61.1.
- Добавлен M67 как опция только после подтверждения HAL; по умолчанию сохранён M68.
- Аудит научен моделировать отложенное применение M67 и проверять последующее движение.
- Добавлены зонные E0/E2-обновления через M67/M68 с предупреждениями.
- Коэффициент осаждения η согласован во всех массово-энергетических расчётах.
- Исправлено двойное применение η в оценке ширины валика.
- Fixed-X no-pause теперь блокируется при меняющемся радиусе, чтобы не генерировать неверную чашу/баллон.
- Исправлены устаревшие имена выгрузок v4291.
- Устранены Streamlit deprecation и Arrow warnings.
- Проверено 466 автоматизированных сценариев, компиляция, AppTest и HTTP health.

# v4.2.9.9 EXPERIENCE PROFILE MERGE

- Изучен внешний PDF-отчет по продолжению проекта.
- Добавлена вкладка `Калибровка по фактическому опыту`.
- Добавлен расчет зонного профиля C/E2/E0/Z по фактической детали и ручным override.
- Добавлена защита от слепого копирования высокой E2 при переутолщенной стенке.
- Добавлено применение профиля к no-pause C-генератору.
- Добавлены JSON/CSV экспорт профиля и отчет.
- Проверено: py_compile, compileall, 207 smoke cases, 213 deep interaction cases, 12 experience-profile cases.

# v4.2.9.7 C CONTINUOUS CONTROLS

- Добавлен режим C-стола: «Одно кольцо, C без остановки, переход C+Z».
- В этом режиме E0/E2 включаются один раз в начале и выключаются только в конце. Обязательное поведение не вынесено в галочку.
- Добавлены галочки отключения лишних действий: паузы, W-ретракт, Z-hop.
- Добавлено поле угла C+Z перехода между слоями.
- Добавлен ручной режим подачи проволоки E2: авто / постоянная / низ-верх.
- При изменении скорости стола E2, энергия и оценочная толщина стенки пересчитываются.
- Добавлены проверки no-pause G-code: нет M68/G4/W/Z-only внутри непрерывного блока.

# История изменений EBAM G-Code Studio

Документ фиксирует развитие приложения по доступной истории проекта.

## Текущая линия разработки

### v4.2.9.6 — REAL C RADIUS LIMIT

- В STL 3D добавлен реальный выбор «Поворотный стол C — кольца по STL» в простом и расширенном режимах.
- Рекомендация по STL теперь не только текстовая: C-режим можно применить и получить G-code с G91 C360.
- Добавлены настройки C-стола для STL: C+/C-, X/Y центра C, B, стартовый C, лимит скорости C, предупреждение малого радиуса и автоограничение F.
- Preview STL C-режима показывает круговые C-кольца в выбранном сечении, а обычные режимы оставляют стрелки направления.
- Тепловой профиль усиленно пересчитывается по геометрии: для круглых высоких STL/баллонов снижается верхняя энергия и добавляется верхняя пауза.
- Зафиксирован урок: любая рекомендация должна иметь реальный UI-выбор, preview и генераторный тест.

### v4.2.8.6 — ROTARY C TABLE

- Добавлен режим `Поворотный стол C — кольца B=0` для параметрических чаш/баллонов.
- G-code строится через `X = X_center_C + R`, `Y = Y_center_C`, `Z = layer` и относительный оборот `G91 C360`.
- Добавлены настройки C: направление C+/C-, центр X/Y, стартовый C, угол B, лимит C град/мин, предупреждение малого радиуса, автоограничение F.
- E0/E2 пересчитываются при снижении линейной скорости из-за лимита C.
- Audit теперь видит активные C-движения и C min/max.
- Для проверка лимитов Бормаш в поворотный стол C режиме учитываются фактические X/Y координаты C-стратегии.
- Добавлены примерные файлы `sample_balloon_rotary_c_table.*`.

### v4.2.8.5 — ROTATIONAL RING SPIRAL

- Добавлены специальные траектории чаш/баллонов: XY-кольца и XY-спираль внутри слоя.
- Старое заполнение сечения сохранено для отката/сравнения.

### v4.2.8.4 — ROTATIONAL 3D PREVIEW

- Исправлен предпросмотр чаш/баллонов и добавлена 3D-модель формы детали.

### v4.2.8.3 — DOCUMENTED HISTORY

- README разделён на текущее руководство, история перенесена в CHANGELOG.

### v4.2.8.2 — ALL TOOLTIPS

- Добавлены подсказки `?` к изменяемым полям.

### v4.2.8.1 — ROTATIONAL VESSELS FIX

- Исправлен `NameError: rotational_shell_summary is not defined`.

### v4.2.8.0 — ROTATIONAL VESSELS

- Добавлен режим параметрических чаш, горшков, стаканов, конусов и шар-баллонов.

### v4.2.7.9 — PATH PLAN FULL DIRECTIONS

- Добавлены все направления змейки и параллельных проходов: Y-, Y+, X-, X+.

### v4.2.7.6 — CONTINUOUS BORMASH

- Объединена continuous-логика с профилем Бормаш и проверками X/Y/Z.

### Заморожено / не используется

- v4.3 режим сварки пока не используется.
- Полный пусконаладочный комплект отложен как задел на будущее.


## v4.2.9.8 C CONTINUOUS CONTROLS QA FIX

- Проведена глубокая проверка взаимодействия параметров C no-pause режима: 420 генерационных кейсов, py_compile/compileall/CLI help.
- Исправлено: ручная E2=0.000 мм/с больше не падает в авторасчёт, а честно записывается в G-code.
- Исправлено: авто-шаг дорожек от измеренной ширины валика теперь применяется в rotational/C/ring/spiral режимах, а не только показывается в UI.
- Проверено: изменение E0, энергии, F, Cmax, Z-шаг, радиальный шаг, E2, напряжение, лимиты, переходный угол, проволока и авто-шаг корректно пересчитывает зависимые параметры.
